name: Sync data

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Force source refresh"
        required: false
        default: false
        type: boolean
  push:
    paths:
      - ".github/workflows/source-sync.yml"
      - "config/competitions.json"
      - "config/sources.json"
      - "data/fixture_schedule.csv.gz"
      - "scripts/source_sync.py.gz"
      - "scripts/source_sync_runner.py"
      - "scripts/detail_parser.py"
      - "scripts/editorial_reports.py"
      - "tests/test_editorial_reports.py"

permissions:
  contents: write

concurrency:
  group: source-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      FORCE_SYNC: ${{ github.event.inputs.force || 'false' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Dependencies
        run: python -m pip install --disable-pip-version-check --quiet requests beautifulsoup4

      - name: Prepare
        shell: bash
        run: |
          gzip -dc scripts/source_sync.py.gz > scripts/source_sync.py
          gzip -dc data/fixture_schedule.csv.gz > data/fixtures.csv

      - name: Sync
        shell: bash
        run: |
          if [ "$FORCE_SYNC" = "true" ]; then
            python scripts/source_sync_runner.py --force
          else
            python scripts/source_sync_runner.py
          fi

      - name: Test editorial rules
        run: python -m unittest discover -s tests -p 'test_editorial_reports.py' -v

      - name: Generate editorial reports
        run: python scripts/editorial_reports.py

      - name: Publish data
        shell: bash
        run: |
          git add data/engine-state.json data/matches.json data/upcoming.json data/competitions.json data/editorial-reports.json
          if git diff --cached --quiet; then
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update data"

          for attempt in 1 2 3; do
            git pull --rebase origin main
            if git push; then
              exit 0
            fi
            sleep $((attempt * 2))
          done
          echo "::error::Could not publish data after three attempts"
          exit 1
